{
  "profiles": [
    {
      "id": "schema_only",
      "name": "Schema-only assessment",
      "description": "Discovery and static tool-schema risk analysis only. No tool calls are executed.",
      "allow_tool_calls": false
    },
    {
      "id": "controlled_readonly",
      "name": "Controlled read-only assessment",
      "description": "Reserved for owned or explicitly authorized MCP servers. Only read-only tool calls may be executed.",
      "allow_tool_calls": true,
      "max_calls": 1
    }
  ]
}
